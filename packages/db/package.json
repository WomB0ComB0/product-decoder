{
  "name": "packages",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "db:gen": "bun run --watch database.gen.ts",
    "db:seed": "bun run --watch src/seed.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "@types/bcrypt": "^6.0.0",
    "bcrypt": "^6.0.0",
    "prisma-dbml-generator": "^0.12.0"
  }
}
